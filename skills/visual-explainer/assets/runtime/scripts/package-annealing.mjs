import {readFile,writeFile,mkdir} from 'node:fs/promises';import {resolve} from 'node:path';
const root=resolve(process.argv[2]??'lessons/multi-operator-annealing');const ir=JSON.parse(await readFile(resolve(root,'lesson.ir.json'),'utf8'));
const stamp=t=>{const ms=Math.round(t*1000);return `${String(Math.floor(ms/3600000)).padStart(2,'0')}:${String(Math.floor(ms/60000)%60).padStart(2,'0')}:${String(Math.floor(ms/1000)%60).padStart(2,'0')},${String(ms%1000).padStart(3,'0')}`;};
await writeFile(resolve(root,'exports/chapters.srt'),ir.scenes.map((s,i)=>`${i+1}\n${stamp(s.start)} --> ${stamp(s.start+s.duration)}\n${s.title}\n${s.narration}\n`).join('\n'));
const titles=ir.scenes.map((s,i)=>`<button data-time="${s.start}">${String(i+1).padStart(2,'0')} ${s.title}</button>`).join('');
await writeFile(resolve(root,'exports/index.html'),`<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>多算子模拟退火 · 讲解视频</title><style>body{background:#0a1220;color:#e9f0fa;font:16px 'Microsoft YaHei',sans-serif;max-width:1100px;padding:28px;margin:auto}h1{font-size:28px}p{color:#9badc7;line-height:1.8}video{display:block;width:100%;border-radius:14px;border:1px solid #2a3b53}nav{display:flex;gap:8px;flex-wrap:wrap;margin-top:22px}button,a{color:#68e1d0}button{background:#111e30;border:1px solid #2a3b53;border-radius:6px;padding:10px;cursor:pointer}footer{margin-top:25px;font-size:13px}</style><h1>多算子模拟退火：三种改法，一套接受规则</h1><p>8 城市旅行商示例 · 中文合成配音与字幕 · 3 分 06 秒 · 1280 × 720 / 24 fps</p><video id="v" controls preload="metadata" poster="poster.png" src="multi-operator-annealing.mp4"></video><nav>${titles}</nav><footer><p>交换、插入、2-opt 以固定概率选择；600 轮真实计算轨迹。坐标与参数为教学构造，有限次运行不保证全局最优。</p><a href="../sources.md">来源与假设</a> · <a href="../quality-report.json">质量报告</a></footer><script>document.querySelectorAll('button').forEach(b=>b.onclick=()=>{const v=document.getElementById('v');v.currentTime=Number(b.dataset.time);v.play();});</script></html>`);
await writeFile(resolve(root,'README.md'),`# 多算子模拟退火讲解视频\n\n打开 exports/index.html 可观看视频并按章节跳转，或直接播放 exports/multi-operator-annealing.mp4。\n\n采用旅行商问题，固定起点 A，欧氏对称距离；三种邻域操作固定等概率选择。seed=42，T0=8，每轮 T←0.99T，共600轮。原路线 ${58.07445318463292.toFixed(2)}，历史最优 ${24.224062567942187.toFixed(2)}，所有数值从 data/trace.json 读取并校验。\n\n中文声音：本机 Microsoft Huihui Desktop 合成语音。字幕采用分场景字符比例估计时间，不声称逐字强制对齐。\n\n此视频使用独立的 annealing-video-0.1 课程配置，不冒用矩阵 LessonIR 0.1。源脚本位于项目 scripts/annealing_video.py 和 scripts/narrate-annealing.ps1。\n`);
console.log('Created chapter player and transcript');
